name: Pipeline
on:
  push:
  schedule:
  - cron: '0 1 * * 1' # Every Monday at 1AM UTC
jobs:
  tests:
    uses: Invoca/ruby-test-matrix-workflow/.github/workflows/ruby-test-matrix.yml@fe2268102d40b5cb6224fadab176c5fa6ab01f61
    with:
      pre-test-hook: "bundle exec rails db:migrate"
      test-command: "bundle exec rake test"
